{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Zarr Metadata extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/packages/extension",
        "${workspaceFolder}/example"
      ],
      "preLaunchTask": "npm: build"
    }
  ]
}
